[build]
  functions = "netlify/functions"
  publish   = "."

[build.environment]
  NODE_VERSION = "18"

[functions]
  node_bundler = "esbuild"

[functions."generate"]
  timeout = 26

[[redirects]]
  from   = "/api/generate"
  to     = "/.netlify/functions/generate"
  status = 200
