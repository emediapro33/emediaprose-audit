[build]
  functions = "netlify/functions"
  publish   = "."

# Route /api/generate → the serverless function
[[redirects]]
  from   = "/api/generate"
  to     = "/.netlify/functions/generate"
  status = 200
